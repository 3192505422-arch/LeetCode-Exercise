class Solution:
    def getMinDistance(self, nums: List[int], target: int, start: int) -> int:
        minDist = float('inf')
        for i, val in enumerate(nums):
            if val == target:
                dist = abs(i - start)
                if dist < minDist:
                    minDist = dist
        return minDist
